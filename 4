package main
import "fmt"
func main() {
	//variables use colon with equal sign
	a :="hello world"
	b := "I will called Buddha"
	//Print and Print they will not have any space between variables
	fmt.Print(b)
    fmt.Print(a)
}